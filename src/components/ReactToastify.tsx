"use client";
import { ToastContainer } from "react-toastify";

const ReactToastify = () => {
  return (
    <>
      <ToastContainer autoClose={500} />
    </>
  );
};

export default ReactToastify;
